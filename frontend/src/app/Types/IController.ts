export interface IController {
  /*
    Every class that implements this should have
    is{$className}() function where $className is the name
    of the class that implements this interface .. Example:
        class Slider implements IController {
            isSlider() {
                return true;
            }
        }
    
    As shown above, this function always returns true;
    */
}
